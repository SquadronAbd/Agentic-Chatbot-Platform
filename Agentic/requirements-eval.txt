# Eval-only dependencies — NOT installed in the production Docker image.
# Run evals locally:
#
#   cd Agentic
#   pip install -r requirements-eval.txt
#   python -m app.tests.evals.run_eval     # LLM-judge harness (no deepeval)
#   pytest app/tests/evals/ -m eval        # deepeval multi-turn suite
#
# These packages conflict with production pins (pydantic, pydantic-settings,
# python-dotenv), so they must stay in a separate install step.

-r requirements.txt

# deepeval and its required version floors
deepeval==4.1.7
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.10.1,<3.0.0
python-dotenv>=1.1.1,<2.0.0

# httpx is already in requirements.txt but deepeval may pull a newer range
# pytest-asyncio drives the async eval tests
pytest-asyncio>=0.23.0
